<h1 class="page-title">検索結果一覧</h1>
<% if @result_count == 0 %>
  <h1 class="sorry-counter"><strong class="emphasis">条件にヒットするルームが見つかりませんでした。<br>条件を変えて再度検索してみてください。</strong></h1>
  <h3><%= link_to "トップページへ", "/", class: "submit-btn btn-sub btn-op top height-add" %></h3>
<% else %>
  <h2 class="result-counter btm"><strong class="emphasis"><%= @result_count %></strong>件ヒットしました。</h2>
  <table class="room-table">
    <div class="room-regi-container">
      <thead>
        <tr>
          <th class="table-title">ルーム画像</th>
          <th>ルーム名</th>
          <th>住所</th>
          <th>ルーム紹介</th>
          <th>1泊当たりの金額</th>
          <th>登録日</th>
        </tr>
      </thead>
      <tbody>
        <% @rooms.each do |room| %>
          <tr>
            <td class="room-regi"><a href =<%= "/rooms/#{room.id}/show" %>><%= image_tag avatar_url(room), class: "room-regi" %></a></td>
            <td><%= link_to room.room_name, "/rooms/#{room.id}/show" %></td>
            <td><%= room.address %></td>
            <td><%= room.description %></td>
            <td><%= room.price %></td>
            <td><%= room.created_at.to_s(:datetime_jp) %></td>
          </tr>
        <% end %>
      </tbody>
    </div>
  </table>
  <h3><%= link_to "トップページへ", "/", class: "submit-btn btn-sub btn-op btm height-add" %></h3>
<% end %>
