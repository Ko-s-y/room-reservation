<h1 class="page-title">予約済みルーム一覧</h1>
<h1>reserveテーブル作ったら訂正</h1>

<table class="room-table">
  <div class="room-regi-container">
    <thead>
      <tr>
        <th>ルーム画像</th>
        <th>ルーム名</th>
        <th>ルーム紹介</th>
        <th>合計金額</th>
        <th>開始日</th>
        <th>終了日</th>
        <th>予約確定日時</th>
      </tr>
    </thead>

    <tbody>
      <%# <% @rooms.each do |room| %>
        <tr>
          <td>@room.image</td>
          <td>@room.room_name</td>
          <td>@room.description</td>
          <td>@reserved.total_price</td>
          <td>@reserved.start_date</td>
          <td>@reserved.finish_date</td>
          <td>@reserved.created_at.to_s(:datetime_jp)</td>
        </tr>
      <%# <% end %>
    </tbody>
  </div>
</table>
<h3><%= link_to "ルーム新規登録", "/rooms/new", class: "submit-btn btn-sub btn-op" %></h3>
